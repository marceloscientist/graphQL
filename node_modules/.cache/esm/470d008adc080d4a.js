let createServer;_dc0‍.w("http",[["createServer",["createServer"],function(v){createServer=v}]]);

const server = createServer((req,res)=>{
    switch (req.url) {
        case '/status':
            response.writeHead(200);
            response.write('Okay');
            response.end();
            return;
    
        default:
            response.writeHead(404);
            response.write('There\'s no route like that! Go back to our home page! ');
            response.end();
            return;
    }
});

server.listen(8000, '127.0.0.1', ()=>{
    console.log('Server is listening at http://127.0.0.1:8000')
})